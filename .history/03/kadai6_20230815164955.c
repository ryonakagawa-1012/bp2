#include <stdio.h>

#define NUM 5

int main() {
    int i;
    int data1[NUM], data2[NUM];

    for (i = 0; i < 5; i++) {
        scanf("%d", &data[i]);
    }

    for (i = 4; i >= 0; i--) {
        printf("data%d : %d\n", i, data[i]);
    }

    return 0;
}