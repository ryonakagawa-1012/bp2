#include <stdio.h>

int main() {
    char string[100];
    int length = 0;
    int Alpha, Number, Else;

    scanf("%s", string);

    while (string[length] != '\0') {
        length++;
    }

    for (int i = 0; i < length; i++) {
    }

    return 0;
}