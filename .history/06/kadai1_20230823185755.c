#include <stdio.h>

int main() {
    char string[100];
    int length;
    int Alpha,

        scanf("%s", string);

    length = 0;
    while (string[length] != '\0') {
        length++;
    }

    return 0;
}