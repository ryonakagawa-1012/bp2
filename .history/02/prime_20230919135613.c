#include <stdio.h>

int main() {
    int num;

    scanf("%d", num);

    for (i = 2; i < num; i++) {
        if (num % i ==
            0) {  // 割り切れた     printf("%d is not prime¥n", number);
                  // exit(0); // プログラムを終了する   } } //
                  // ループを回りきった printf("%d is prime¥n", number);
        }